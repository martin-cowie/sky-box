{
  "name": "sky-box",
  "version": "1.0.0",
  "description": "",
  "config": {
    "outDir": "build"
  },
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./$npm_package_config_outDir/",

    "prebuild": "rm -rf ./$npm_package_config_outDir",
    "build": "tsc",
    "postbuild": "cp -f index.html package.json ./$npm_package_config_outDir"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "moment": "^2.24.0",
    "node-ssdp": "^4.0.0",
    "typeof-arguments": "^5.1.1",
    "xmldom": "^0.3.0",
    "xpath": "0.0.27"
  },
  "devDependencies": {
    "electron": "^8.1.1",
    "typescript": "^3.8.3"
  }
}
